{
  "name": "@web-craft-war/game-logic",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@web-craft-war/shared-types": "*"
  },
  "devDependencies": {
    "@web-craft-war/tsconfig": "*",
    "typescript": "^5.7.0"
  }
}
